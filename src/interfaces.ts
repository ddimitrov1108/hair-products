export interface QuestionsForm {
  step: number;
  isLoading: boolean;
}

export interface Questions {
  hairType: string;
  hairWash: string;
  hairProducts: string;
  hairTroubling: string;
  hairColor: string;
}
